var roles = [
    {name: "harvester", roleClass: require('roles.harvester')},
    {name: "upgrader", roleClass: require('roles.upgrader')},
    {name: "builder", roleClass: require('roles.builder')}
];

module.exports = roles;